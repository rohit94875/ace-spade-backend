package com.acespade.model.enums;

/**
 * Rank ordering by ordinal: TWO(0) < THREE(1) < ... < ACE(12).
 * Higher ordinal = higher card value.
 */
public enum Rank {
    TWO, THREE, FOUR, FIVE, SIX, SEVEN, EIGHT, NINE, TEN, JACK, QUEEN, KING, ACE
}
